import origemBox from './origem-box.vue';

describe('origemBox', () => {
  it('should be a origemBox box', () => {
    expect(origemBox.name).toEqual('origemBox');
  });
  // it('Should receive origemBox data', () => {
  //   // DEFINIR PROPS A SEREM RECEBIDAS
  // });
  // it('Should render origemBox data', () => {
  //   // DEFINIR DATA A SER RENDERIZADA
  // });
});
