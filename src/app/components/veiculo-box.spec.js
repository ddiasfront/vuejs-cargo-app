import veiculoBox from './box-grid.vue';
import {shallowMount} from '@vue/test-utils';

describe('veiculoBox', () => {
  it('should be a veiculoBox box', () => {
    expect(veiculoBox.name).toEqual('veiculoBox');
  });
  it('Should receive veiculoBox data', () => {
    // DEFINIR PROPS A SEREM RECEBIDAS
  });
  it('Should render veiculoBox data', () => {
    // DEFINIR DATA A SER RENDERIZADA
  });
});
