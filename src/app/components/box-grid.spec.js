import boxGrid from './box-grid.vue';

describe('Grid Box', () => {
  it('should be a boxGrid box', () => {
    expect(boxGrid.name).toEqual('boxGrid');
  });
  // it('Should receive boxGrid data', () => {
  //   // DEFINIR PROPS A SEREM RECEBIDAS
  // });
  // it('Should render boxGrid data', () => {
  //   // DEFINIR DATA A SER RENDERIZADA
  // });
});
